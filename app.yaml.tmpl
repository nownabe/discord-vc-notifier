runtime: go113
env_variables:
  DISCORD_BOT_TOKEN: "${DISCORD_BOT_TOKEN}"
  DISCORD_CHANNEL_ID: "${DISCORD_CHANNEL_ID}"
inbound_services:
  - warmup
instance_class: F1

automatic_scaling:
  min_instances: 1
  max_instances: 1
